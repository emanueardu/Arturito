--- a/src/robertito/robertito/node_manager.py
+++ b/src/robertito/robertito/node_manager.py
@@ -25,7 +25,7 @@ class RobertitoNodeManager(Node):
             "wake_greeting", "Hola, ¿en qué puedo ayudarte?"
         ).get_parameter_value().string_value
         self._startup_greeting = self.declare_parameter(
-            "startup_greeting", "Hola, buenos días. Soy Robertito."
+            "startup_greeting", ""
         ).get_parameter_value().string_value
         self._happy_expression = self.declare_parameter(
             "wake_expression", "happy"
@@ -43,7 +43,7 @@ class RobertitoNodeManager(Node):
         self.get_logger().info(
             f"Node manager escuchando activaciones en {self._wake_topic}"
         )
-        if self._startup_greeting:
+        if self._startup_greeting.strip():
             self._publish_tts(self._startup_greeting)

     def _on_wake_event(self, msg: Bool) -> None:

--- a/src/robertito/launch/robertito_launch.py
+++ b/src/robertito/launch/robertito_launch.py
@@ -60,6 +60,11 @@ def generate_launch_description() -> LaunchDescription:
         default_value="jarvis",
         description="Modo de voz: normal o jarvis.",
     )
+    startup_greeting_arg = DeclareLaunchArgument(
+        "startup_greeting",
+        default_value="",
+        description="Saludo inicial del node manager. Vacío por defecto (usa startup_message de voice_synth).",
+    )

     wake_topic = "/wake_word/detected"
     assistant_say_topic = "/assistant/say"
@@ -72,6 +77,7 @@ def generate_launch_description() -> LaunchDescription:
     face_detector_start_active = LaunchConfiguration("face_detector_start_active")
     voice_mode = LaunchConfiguration("voice_mode")
+    startup_greeting = LaunchConfiguration("startup_greeting")

     eyes_node = Node(
         package="robertito",
@@ -229,7 +235,7 @@ def generate_launch_description() -> LaunchDescription:
             {"wake_topic": wake_topic},
             {"eyes_expression_topic": "robertito/eyes_expression"},
             {"tts_topic": assistant_say_topic},
-            {"startup_greeting": "Hola, buenos días. Soy Robertito."},
+            {"startup_greeting": startup_greeting},
             {"wake_greeting": "Hola, ¿en qué puedo ayudarte?"},
         ],
     )
@@ -245,6 +251,7 @@ def generate_launch_description() -> LaunchDescription:
     ld.add_action(face_detector_active_arg)
     ld.add_action(voice_mode_arg)
+    ld.add_action(startup_greeting_arg)
     ld.add_action(eyes_node)
     ld.add_action(voice_synth)
     ld.add_action(wake_listener)
